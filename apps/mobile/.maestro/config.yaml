# Maestro workspace configuration for A-hub mobile
# Run all flows: maestro test .maestro/flows/
# Run single flow: maestro test .maestro/flows/auth/login.yaml
# Run smoke suite: maestro test .maestro/smoke.yaml

flows:
  - "flows/**/*.yaml"
